# Offline profile: StubTranscriptionService replaces the real Cloud STT provider.
# Used for regression tests and load tests so no API key, network, or spend is involved.
# Activate with: SPRING_PROFILES_ACTIVE=stub
stt.stub.latency-millis=250
logging.level.comp3011.assignment1=DEBUG
